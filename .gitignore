# dependencies
node_modules
.pnp
.pnp.js

# build output
.next
out
next-env.d.ts

# image masters
# The original generated PNGs are ~75 MB and are never served — only the
# compressed JPEGs under public/img/ ship. Keep the masters locally so the
# images can be re-cropped, but do not push them.
_sources/

# local env
.env
.env.local
.env.*.local

# vercel
.vercel

# editor / OS
.DS_Store
Thumbs.db
*.swp
.idea
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
